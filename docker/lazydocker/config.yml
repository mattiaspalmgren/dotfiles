gui.returnImmediately: true
customCommands:
  images:
    - name: os
      attach: true
      command: "docker image inspect --format '{{printf \"{{.Os}}/{{.Architecture}}\"}}' {{ .Image.ID }}"
      serviceNames: []
